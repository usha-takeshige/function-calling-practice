import { describe, it } from 'vitest'

/**
 * DataTable コンポーネントのテスト
 * Props: processedDataset, sortOrder を受け取り描画するPresentational Component
 *        ヘッダークリック時に onHeaderClick を呼ぶ
 */
describe('DataTable', () => {
    it.todo('ヘッダー行とデータ行が正しく描画される')
    it.todo('列ヘッダーをクリックするとonHeaderClickが呼ばれる')
    it.todo('昇順ソート中の列ヘッダーに昇順アイコンが表示される')
    it.todo('降順ソート中の列ヘッダーに降順アイコンが表示される')
    it.todo('データが空のとき行が描画されない')
})
