import { describe, it } from 'vitest'

/**
 * FileUpload コンポーネントのテスト
 * Props: onFileSelect(file: File) を受け取るPresentational Component
 */
describe('FileUpload', () => {
    it.todo('ドラッグ＆ドロップエリアが描画される')
    it.todo('CSV以外のファイルをドロップするとエラーメッセージが表示される')
    it.todo('CSVファイルをドロップするとonFileSelectが呼ばれる')
})
