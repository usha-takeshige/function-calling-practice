import { describe, it } from 'vitest'

/**
 * App コンポーネントの統合テスト
 * useTableData フックを使用し、FileIO との連携を含む全体フロー
 */
describe('App (Integration)', () => {
    it.todo('初期表示ではドラッグ＆ドロップエリアのみが表示される')
    it.todo('CSVファイルをドロップするとデータテーブルが表示される')
    it.todo('フィルタ入力後にテーブルの表示行が絞り込まれる')
    it.todo('列チェックボックスをOFFにすると該当列がテーブルから消える')
    it.todo('エクスポートボタンクリックでdownloadCSVが呼ばれる')
})
